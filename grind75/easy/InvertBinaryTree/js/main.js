var invertTree = function(root) {
    
    // split the array by 0+0 to the power of 2; 1+0 to the power of 2 then 1+1 to the power of 2 for the length of array
    // condintional if i is < 2 split char at 0 and 
    // so for loop with slice for chunk length
    // first chunk will always 

};